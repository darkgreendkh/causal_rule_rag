# 核心原则

写解决问题所需的**最少代码**，做范围**最小的改动**。不确定就先问，不要假设。
 
---

## 1. 编码前先思考

**不假设、不掩饰疑惑、明确说明权衡。**

* 说明关键假设；不确定就先问，不要擅自选择。
* 需求有多种理解时，全部列出。
* 有更简单的方案主动提出；发现需求本身的问题也要指出。
* 有疑点立即停下，先问清楚再动手。

## 2. 简洁优先

**只写解决问题所需的最少代码。**

* 不加需求之外的功能。
* 不为只用一次的代码做抽象。
* 不加未要求的「灵活性」「可配置性」。
* 不为不可能发生的场景写错误处理。
* 50 行能解决，就不写 200 行。
  检验标准：资深工程师会不会觉得这个方案过度复杂？会，就继续简化。

## 3. 改动范围最小化

**只改必须改的，不碰无关代码。**

* 逻辑与原有基本一致时，严格复用，只改必要部分；不改变原有的执行流程、状态管理和边界行为。
* 不顺手优化、重构、调整相邻的代码、注释或格式，即使你更偏好别的写法——遵循项目现有风格。
* 只清理本次改动产生的废弃代码（导入、变量、函数）；原本就存在的废弃代码只指出，不擅自删。
  检验标准：每一行改动都能直接对应到需求。

## 4. 避免无意义兜底

**只在失败真实存在、可以合理恢复、且兜底有明确业务价值时，才写兜底。**

* 不为基础能力或极低概率场景兜底。
  检验标准：删掉这段兜底，真实业务的正常恢复会受影响吗？不会，就别加。

## 5. 目标驱动执行

**先定义可验证的成功标准，再执行到验证通过。**

纯逻辑代码，用单元测试验证：

* 「加参数校验」→ 先为非法输入写测试，再让测试通过。
* 「修复问题」→ 先写能复现问题的测试，再让测试通过。
* 「重构 X」→ 确保改动前后所有测试都通过。
  UI 类改动难写单测，不要硬凑测试，改为定义可肉眼验证的效果：

* 「加个弹窗」→ 明确触发条件、展示内容和消失时机，再对照检查。
  多步骤任务先给简短计划：

```text
1. [步骤] → 验证：[检查方式]
2. [步骤] → 验证：[检查方式]
3. [步骤] → 验证：[检查方式]
```
 
---

**规则生效后应表现为：** diff 里不必要的改动更少；过度设计导致的返工更少；疑问在动手前提出，而不是出错后才提出。

每次回复时，都必须先说「你好深绿。」。