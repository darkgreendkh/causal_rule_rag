# 交付核对

核对日期：2026-09-14。本记录是工程与排版验收。第一层自动实验的结果单列于末节，第二层人工标注实验尚未开展。

## 程序与资料

- 后端单元、规则、覆盖、路径与API回归：271项通过，Ruff通过（新增下一步候选与规则校验去重两项回归）。
- 真实Neo4j集成：独立engineering-test命名空间通过，测试对象已清理，原知识库保留。
- 前端：18项测试通过，lint与生产构建通过；Edge浏览器真实接口验收记录见screenshots/acceptance.md。
- 三模式问答均得到真实模型接口HTTP200；有序流程首错、外部未知、成本1的插入修复、预览与应用、已办历史持久化通过。
- Windows CUDA运行及78个已安装Python包的依赖兼容检查通过。
- 36份政策逐文件哈希与逐单元覆盖完整；policy-coverage.json保存1481个覆盖单元与115个事项。

## 文稿与视觉

- 七章正文44639汉字；统计排除标题、图题、表格与围栏伪代码，不用占位数据凑字数。
- 20条顺序编码参考文献，均有原件或核验说明；20个编号全部在正文出现，无残留字母引用键。
- 10张方法图、5张正文系统截图、7张由实验数据生成的结果图、4张待标注实验占位图，共26个Word内嵌图片。所有图片路径存在；方法图保留DOT/SVG，结果图可由脚本按JSON重绘。
- 70个OMML数学对象保留可编辑结构，未将其替换成不可编辑的截图。
- Microsoft Word实际导出94页PDF，并渲染每页PNG检查。表格为三线表，短表整体同页。补入实验结果后重新渲染，第六章新增表图逐页目视。
- 中英文摘要、目录域、分节页码、正文、图表、参考文献和致谢均检查；封面个人信息为空，Word编辑者元数据已清空。清理元数据前后word/document.xml字节不变。
- documents工具的artifact渲染也已执行并逐页交叉检查。该引擎出现OMML漏显、列宽塌缩及分节页码差异；这些问题未出现在Word原生输出，交付排版依据为Microsoft Word。

原生排版QA图片和PDF保存在 `.runtime/thesis-qa/`，不混入正式论文或科研结果。论文更新或填入实验结果后须重新更新目录并进行分页检查。

## 第一层自动实验

- E04—E07、E09—E11 由 `backend/experiments/run_all.py` 一次运行产出，结果与运行元数据写入
  `docs/thesis/experiments/`；运行只读 `data/` 与向量缓存，不连 Neo4j、不调用生成模型。
- 参考计划求解覆盖 72 个多动作事项中的 63 个，其余 9 个因“规则有效期尚未核实”返回未知，分母保留。
- 三处与预期不一致的结果已按原样写入正文：RPC/SCS 阈值未生效、开放查询召回下降、路径语义项为负作用。
- 运行中发现并修复两处实现缺陷：下一步候选包含已完成动作（Top-1 0.000→0.9274）；
  同一规则在多条路径上重复返回（单次问答 711 条→8 条）。两者均补了回归测试并在真实接口上复验。
- 第二层（E01—E03、E08）需要独立双人标注，尚未开展；正文相应表图保持待标注标记。
