# 前端真实联调验收记录

验收日期：2026-09-14。此记录为工程联调与界面验收，不构成论文第六章的科研实验结果。

## 环境与数据

- 前端 http://localhost:5173，后端 http://localhost:8000；使用真实 Edge / Playwright，无 API mock。
- 当前构建标识：`de0d04a9a1034b9e98b2da5ed4579c3d`。36 份政策、8 篇研究文献、1,481 个语义单元、115 个事项、288 条启用规则、233 个动作；图谱 4,503 节点、7,801 关系、2,341 条层次社区记录。此处为当前构建统计，不代表抽取准确率或办理流程完整率。
- 截图视口 1440 × 960，像素密度 1.5；图片 2160 × 1440，均为当前真实界面局部视口。

## 已通过的验收

1. 概览显示真实全库统计、115 个事项及资料缺口；长缺口清单折叠后不阻挡事项入口。
2. 文档研究区显示原始政策资料、实际条款、事项关联规则及来源证据；已读取《社会保险经办条例》第三十二条，并核查候选抽取按钮的事项与来源限制。旧上传库的 legal_sample.md 仍可读取。
3. 养老个人账户事项图谱：macro 31 节点 / 30 关系，micro 12 / 13，fused 43 / 47；社区筛选、边证据与方向可见；JSON 与 GraphML 导出可用，GraphML 保留 directed 与研究层元数据。
4. 流程仅有结果登记步骤时校验 violated；缺少外部确认时修复 unknown。外部事实明确确认后，修复建议插入查询步骤，代价 1；经采用计划、预览、应用两步，最终校验 satisfied 且目标满足。已办历史两条，无撤销按钮；撤去外部确认时后续动作保持 unknown 且无预览。
5. 真实 causal 问答 HTTP 200，run_id `af56f3e05d144a1998b447f85705bcad`；请求包含两条 completed_steps 与当前事实，返回 1 个来源、5 条有向路径、271 条规则或路径诊断。未进入答案证据预算的检查显示“仅诊断，未用于回答”。答案确认查询免费并正确描述两步已办状态。
6. 同一研究资料库的 vector 与 hybrid 均真实调用成功，各返回 8 个来源；run_id 分别为 `bb838e53b0e14f96828440363205b1d2`、`559f11d447a44afab9f664b4e4387fff`。该检查仅确认数据集接通，不比较方法质量。
7. 旧 v1 会话成功迁移至 v2，旧条目清除、新会话保留 1 条真实历史；研究会话保存 dataset、事实与 completed_steps。切换上传资料库后事项与流程输入停用。
8. 答案通过 react-markdown 与 remark-gfm 渲染；真实 causal 答案生成 4 个标题、4 处强调、1 张表格，表格可横向滚动，页面无横向溢出。真实旧 hybrid 纯文本逐字保留（忽略渲染空白）。显式 skipHtml 禁用原生 HTML，无 dangerouslySetInnerHTML。
9. 前端测试 18 / 18 通过，lint 通过，TypeScript 与 Vite 生产构建通过；验收无 pageerror、无失败 API 响应。

## 验收边界

- 真实库未执行规则审核状态变更、候选抽取、文档移除或重建；这些按钮仅核查可见性、证据及上下文约束，破坏性路径由隔离后端测试负责。
- 新构建已核对存量房公积金贷款真实政策字段：申请金额可编辑；申请时贷款最高限额显示“未知”且只有只读 output，无输入控件。界面填写 500000 元仅为本次模拟测试输入，校验请求包含 requested_loan，未携带 loan_maximum_at_application；请求 HTTP 200。该上限需由适用政策配置维护，未由申请人伪造。
- 问答截图使用后端已持久化的真实 run 重新展示，未伪造回答或接口；无新增科研实验样本和性能结论。

## 截图与机器记录

- `overview.png`：概览与全库统计。
- `documents.png`：原始资料与来源条款。
- `graph.png`：具体事项微观有向图与证据。
- `qa.png`：真实规则增强问答，含 Markdown 排版与路径。
- `workflow.png`：流程修复、预览及模拟操作。
- `rules.png`：规则证据与候选审核界面。
- `loan-policy.png`：新增真实贷款事项的申请金额与只读未知政策参数局部截图。
- `acceptance.json`：精简请求上下文、状态、run_id 和断言，规则检查仅按相同标识 / 状态 / 答案使用情况聚合计数，不存放完整来源正文。

版本说明：概览、图谱与贷款政策参数截图已刷新至当前构建；原三模式问答、流程截图及其真实 run_id 属于构建 `e5df93e03ddd428f8e5cf80bd6c87b32`，作为历史验收保留，未重复调用模型。新增字段验证与最终数字记录在 current_build_refresh / policy_form。
